apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-ingress
spec:
  ingressClassName: public
#  tls:
#    - hosts:
#        - dev-auth-api.jifelog.com
#      secretName: dev-jifelog-wildcard-tls
  rules:
    - host: dev-gateway.jifelog.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway-service
                port:
                  number: 8080